# Environment variables for the Brill Backend API

# Server configuration
PORT=5000
NODE_ENV=development # Set to 'production' for production builds

# CORS configuration
CORS_ORIGIN=* # Or specify your frontend URL: http://localhost:3000

# Session management
SESSION_SECRET=your-super-secret-session-key

# Database (replace with your actual database URL)
DATABASE_URL="postgresql://user:password@host:port/database"

# Firebase Admin SDK configuration (replace with your credentials)
# Option 1: Path to your service account key file
# GOOGLE_APPLICATION_CREDENTIALS="path/to/your/serviceAccountKey.json"

# Option 2: Directly embedding the credentials as a JSON string
# FIREBASE_CONFIG='{"type":"service_account",...}'

# Paystack API keys
PAYSTACK_PUBLIC_KEY=your-paystack-public-key
PAYSTACK_SECRET_KEY=your-paystack-secret-key

# Google Maps API key (for geolocation services)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Resend API key (for email services)
RESEND_API_KEY=your-resend-api-key

# HERE API KEY (for advanced route optimization)
HERE_API_KEY=your-here-api-key

# Default admin user credentials (optional)
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=your-secure-password
